# CrisisMapper — environment variables
# Copy to .env and fill in values; never commit .env
# Nuxt 4 maps runtimeConfig keys via NUXT_* prefix:
#   runtimeConfig.dbUrl           → NUXT_DB_URL
#   runtimeConfig.supabaseServiceKey → NUXT_SUPABASE_SERVICE_KEY
#   runtimeConfig.public.supabaseUrl → NUXT_PUBLIC_SUPABASE_URL
#   runtimeConfig.public.supabaseAnonKey → NUXT_PUBLIC_SUPABASE_ANON_KEY

# Supavisor pooler (Settings → Database → Connect → Transaction mode, port 6543)
# Append ?prepareThreshold=0 and URL-encode % in password as %25
NUXT_DB_URL=postgresql://postgres.xxxx:PASSWORD@aws-0-eu-west-1.pooler.supabase.com:6543/postgres?prepareThreshold=0

# Supabase (Settings → API)
NUXT_SUPABASE_SERVICE_KEY=
NUXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co
NUXT_PUBLIC_SUPABASE_ANON_KEY=

# Phase 2 — Demo crisis UUID (seeded via supabase/migrations/20260507000000_seed_demo_crisis.sql)
NUXT_PUBLIC_DEMO_CRISIS_ID=

# Phase 4 — Vision classification
# Primary: Groq Llama 4 Scout (only Groq vision model since Maverick was deprecated 2026-03-09)
# Fallback: Gemini 2.5 Flash via its OpenAI-compatible endpoint
# Both use the same `openai` SDK — different baseURL + apiKey.
NUXT_GROQ_API_KEY=
NUXT_GROQ_VISION_MODEL=meta-llama/llama-4-scout-17b-16e-instruct
NUXT_GEMINI_API_KEY=
NUXT_GEMINI_VISION_MODEL=gemini-2.5-flash

# Phase 9 — Reporter identity (REQUIRED in production)
# HMAC salt for pseudonymous reporter ids. Generate once with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# KEEP IT STABLE FOREVER: rotating the salt changes every device hash, which
# orphans all reporter trust scores, badges, and leaderboard history.
# Without it, production reports are stored anonymously (reporter_id NULL) and
# the profile/leaderboard features stay empty. Dev has a built-in fallback.
NUXT_REPORTER_SALT=

# Phase 8 — Custom Language Pack machine translation (optional)
# Self-hosted LibreTranslate keeps crisis terminology off external vendors:
#   docker run -ti --rm -p 5000:5000 libretranslate/libretranslate
# Without it /admin/languages still works via the manual fill-in workflow.
NUXT_LIBRE_TRANSLATE_URL=http://localhost:5000
NUXT_LIBRE_TRANSLATE_API_KEY=
