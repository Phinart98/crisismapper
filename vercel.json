{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nuxtjs",
  "installCommand": "npm install --os=linux --cpu=x64 sharp && npm install",
  "crons": [
    { "path": "/api/health", "schedule": "0 4 * * *" }
  ]
}
